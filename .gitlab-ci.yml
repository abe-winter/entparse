cache:
  paths:
    - env

python2:
    script:
        - virtualenv env --system-site-packages && source env/bin/activate
        - cd entparse
        - pip install -e .
        - py.test test/test.py
